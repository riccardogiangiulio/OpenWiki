<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/style/output.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
</head>
<body class="bg-gradient-to-br from-blue-900 to-blue-500 h-screen flex items-center justify-center">
    <div class="backdrop-blur bg-opacity-50 bg-white p-8 rounded-lg shadow-lg w-full max-w-md animate__animated animate__zoomInDown">
        <!-- Logo or Header -->
        <div class="text-center mb-6">
            <img src="/images/logo.jpg" alt="Logo" class="mx-auto mb-4 w-24 h-24">
            <h1 class="text-2xl font-bold">Benvenuto su OpenWiki</h1>
            <p class="text-gray-600">Accedi per esplorare e salvare i tuoi articoli preferiti</p>
        </div>
        <form id="loginForm" class="flex flex-col gap-4">
            <input id="email" type="email" placeholder="Email" class="input input-bordered w-full p-2 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500">
            <input id="password" type="password" placeholder="Password" class="input input-bordered w-full p-2 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500">
            <div id="error-message" class="text-red-500 mt-4 hidden">Credenziali errate. Riprova.</div>
            <button type="submit" class="btn btn-primary w-full bg-blue-600 text-white p-2 rounded-md hover:bg-blue-500">Accedi</button>
        </form>
        <!-- separator -->
        <hr class="my-6 border-gray-300">
        <h6 class="text-center text-gray-500">Se non hai ancora un account</h6>
        <hr class="my-2 border-gray-300">
        <div class="mx-auto max-w-6xl w-full flex flex-col gap-4">
            <div class="flex justify-between items-center">
                <!-- modal button -->
                <button onclick="openCreateUserModal()" class="btn btn-primary w-full bg-blue-600 text-white p-2 rounded-md hover:bg-blue-500 flex items-center justify-center">Registrati</button>
            </div>
            <!-- user modal -->
            <dialog id="userModal" class="modal">
                <!-- modal box -->
                <div class="modal-box flex flex-col gap-4 bg-white p-6 rounded-md shadow-lg">
                    <h3 class="font-bold text-lg text-center">Registrati</h3>
                    <form id="userForm" class="flex flex-col gap-4">
                        <!-- error placeholder -->
                        <input name="firstName" placeholder="Nome" type="text" class="input input-bordered w-full p-2 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500">
                        <input name="lastName" placeholder="Cognome" type="text" class="input input-bordered w-full p-2 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500">
                        <input name="email" placeholder="Email" type="text" class="input input-bordered w-full p-2 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500">
                        <input name="password" placeholder="Password" type="password" class="input input-bordered w-full p-2 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500">
                        <input name="passwordConfirmation" placeholder="Conferma password" type="password" class="input input-bordered w-full p-2 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500">
                        <div class="modal-action flex justify-end">
                            <button type="button" class="btn bg-gray-300 text-gray-700 p-2 rounded-md hover:bg-gray-400" onclick="userModal.close()">Chiudi</button>
                            <button type="submit" class="btn btn-primary bg-blue-600 text-white p-2 rounded-md hover:bg-blue-500 ml-2">Salva</button>
                        </div>
                    </form>
                </div>
                <!-- backdrop -->
                <form method="dialog" class="modal-backdrop">
                    <button>Chiudi</button>
                </form>
            </dialog>
        </div>
    </div>
    <script src="//cdnjs.cloudflare.com/ajax/libs/validate.js/0.13.1/validate.min.js"></script>
    <script src="/scripts/signIn.js" type="text/javascript"></script>
    <script src="/scripts/login.js"></script>
</body>
</html>
